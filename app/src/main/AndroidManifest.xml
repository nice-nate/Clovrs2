<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.groupn.clovrs2">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".Registration" />
        <activity android:name=".ManageWorkspaces" />
        <activity android:name=".CMUI" />
        <activity android:name=".MeetingArchive" />
        <activity android:name=".Reset" />
        <activity android:name=".UpcomingMeeting" />
        <activity android:name=".EditWorkspace" />
        <activity android:name=".Settings" />
        <activity android:name=".Upload" />
        <activity android:name=".Contacts" />
        <activity android:name=".CreateMeetingInivte" />
        <activity android:name=".CustomizeAvatar" />
        <activity android:name=".Calendar" />
        <activity android:name=".About" />
        <activity android:name=".Privacy" />
        <activity android:name=".UpdateInformation"></activity>
    </application>

</manifest>